/**
 * Landing route group layout — disponível para futura expansão
 * (ex: /home, /pricing, /about dentro do grupo (landing))
 */
export default function LandingGroupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}

